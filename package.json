{
  "name": "siddaganga-hospital-webapp",
  "version": "2.0.0",
  "description": "Siddaganga Hospital medicine delivery/tracking system - Node.js/Express + MySQL port of the original Flask/SQLite app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd client && npm install && npm run build",
    "postinstall": "echo 'Backend deps installed. Run: cd client && npm install && npm run build'"
  },
  "dependencies": {
    "@google/genai": "^0.9.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-mysql-session": "^3.0.3",
    "express-session": "^1.18.0",
    "mysql2": "^3.11.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
